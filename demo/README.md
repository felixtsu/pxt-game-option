# 选项示例

演示 `pxt-game-option` 扩展：弹出 2 个选项供玩家选择。

## 导入方式

MakeCode → **导入** → **导入 URL**：

```
https://github.com/felixtsu/pxt-game-option/tree/main/demo
```

## 源码

本目录 `main.ts` 先 `create` 两个选项，再 `choose2` 传入这两个变量。
